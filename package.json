{
  "name": "edge-device",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "device": "az iot hub device-identity create -d $DEVICE -n $HUB --edge-enabled -oyaml",
    "clean": "rm -f ./cert/*.pem && az iot hub device-identity delete -d $DEVICE -n $HUB -oyaml",
    "docker": "docker-compose -p edge up -d",
    "docker:stop": "docker-compose -p edge stop && docker-compose -p edge rm --force"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {}
}
